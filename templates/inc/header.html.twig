<header>
    <ul>
        <li><a href="{{ path('app_menu') }}">Le logo</a></li>
        {% if app.user %}
        <li><a>Quizz</a></li>
        <li><a>Mes Quizz</a></li>
        <li><a>Classement Général</a></li>
        <li><a href="{{ path('app_amis') }}">Vos amis</a></li>
            {% if is_granted('ROLE_ADMIN')  %}
            <li><a href="{{ path('app_admin') }}">Espace Administrateur</a></li>
            {% endif %}
        {% else %}
        <li><a href="{{ path('app_login') }}">Se connecter</a></li>
        <li><a href="{{ path('app_register') }}">S'inscrire</a></li>
        {% endif %}
        {% if app.user %}
        <li><a href="{{ path('app_logout') }}">Me déconnecter</a></li>
        {% endif %}
    </ul>
</header>